{
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pg": "^8.15.6",
    "todo-express-ts": "file:",
    "typeorm": "^0.3.23",
    "zod": "^3.24.4"
  },
  "name": "todo-express-ts",
  "version": "1.0.0",
  "description": "This repository provides a structured specification for a ToDo list backend, including all essential API endpoints, request/response formats, and authentication flow. The goal is to serve as a reference blueprint for implementing the same project across multiple backend frameworks.",
  "main": "src/app.js",
  "scripts": {
    "build": "rm -r ./build && tsc",
    "test": "rm -r ./build && tsc && ./run-dev.ps",
    "dev": "nodemon --delay 5",
    "migrate": "tsc && build/database/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.17",
    "nodemon": "^3.1.10",
    "typescript": "^5.8.3"
  }
}
